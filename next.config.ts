import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ["i.pinimg.com",'images.unsplash.com','plus.unsplash.com','img.freepik.com','images.pexels.com','gravatar.com','lh3.googleusercontent.com']
    }
  };

export default nextConfig;
